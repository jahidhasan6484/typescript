// Ternary Operator

const personAge: number = 23;

if (personAge >= 18) {
    console.log("Person is an adult")
} else {
    console.log("Person is still a child")
}

const isAdult = personAge >= 18 ? "Person is an adult" : "Person is still a child"
console.log(isAdult)